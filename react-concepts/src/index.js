import ReactDOM from 'react-dom';
import React from 'react';
import App from './App';




//Create class based and function based components with styling them? 
// Use props and states in your application? 
// in App.js

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);


